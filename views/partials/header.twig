<!DOCTYPE html>
<html {{ fn('language_attributes') }} class="{{ fn('get_body_class')|join(' ') }} browser--no-js">
	<!-- Custom Design and Development by -->
	<!-- Say Hello GmbH - https://sayhello.ch -->
	<head>
		<meta charset="{{ fn('bloginfo', 'charset') }}">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<link rel="profile" href="http://gmpg.org/xfn/11">
		{{ fn('wp_head') }}
	</head>
<body {{ fn('body_class') }}>
{{ fn('do_action', 'sht_after_body_open') }}
