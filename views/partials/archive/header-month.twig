<section class="c-archiveheader c-archiveheader--month">
	<h1 class="c-archiveheader__title c-main__title"><a href="{{ fn('get_month_link', fn('get_the_time', 'Y'), fn('get_the_time', 'm')) }}">{{ _x('Beiträge aus %s', 'Month loop header title', 'sht')|format(fn('get_the_time', 'F Y')) }}</a></h1>
	{% if fn('get_the_archive_description') is not empty %}
		<div class="c-archiveheader__description">{{ fn('get_the_archive_description') }}</div>
	{% endif %}
</section>
